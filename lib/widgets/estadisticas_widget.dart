import 'package:flutter/material.dart';

class Estadisiticas extends StatelessWidget {
  const Estadisiticas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Estoy en vista de las estadisticas"),
      ),
    );
  }
}